import React from 'react';
import {render} from 'react-dom';

var Export=React.createClass({
    render:function(){

        return(
            <div className="Export">it is export component</div>
        );
    }
});
module.exports=Export;